% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poststratify-prep.R
\name{besd_poststrat_frame}
\alias{besd_poststrat_frame}
\title{Prepare a poststratification frame for use with BeSD models}
\usage{
besd_poststrat_frame(df, fit, mapping = NULL, pop_col)
}
\arguments{
\item{df}{A data frame or tibble. Each row should be a unique covariate
combination. The \code{pop_col} column contains the population count for
that cell (e.g. number of people with that combination of characteristics).}

\item{fit}{A \code{besd_fit} or \code{besd_fit_by_country} object from
\code{\link[=besd_regress]{besd_regress()}}.}

\item{mapping}{Optional named character vector mapping column names in \code{df}
to predictor names used in the fitted model. Names are the user's column
names in \code{df}; values are the corresponding predictor names as used in the
model. Uses the same convention as the \code{mapping} argument in \code{\link[=as_besd]{as_besd()}}.}

\item{pop_col}{String. Name of the column in \code{df} containing population
cell counts (e.g. number of census respondents in that cell). Must be
numeric, non-negative, and have no missing values. Note: this is
distinct from survey sampling weights (\code{weight_col} in \code{\link[=as_besd]{as_besd()}});
see Details.}
}
\value{
A \code{besd_poststrat_frame} object: a validated, encoded tibble with a
\code{.row_id} column appended. The \code{pop_col}, \code{country_col}, and \code{level_map}
are stored as attributes for downstream use by \code{\link[=besd_poststratify]{besd_poststratify()}}.
}
\description{
Validates, maps, and encodes a poststratification data frame (e.g. from
census or administrative data) for use with \code{\link[=besd_fitted_probs]{besd_fitted_probs()}} and
\code{\link[=besd_poststratify]{besd_poststratify()}}. Each row must represent a unique covariate
combination (cell) with an associated population count in \code{pop_col}.
Predictor columns are encoded to match the internal representation used
during model fitting, using the metadata stored in the \code{fit} object.
}
\section{Sampling weights vs population counts}{

Survey sampling weights (specified via \code{weight_col} in \code{\link[=as_besd]{as_besd()}}) correct
for unequal selection probabilities within the survey sample and are used
during model fitting. Population cell counts (\code{pop_col} here) are used only
during poststratification to reweight model predictions to the target
population structure. These are conceptually distinct and should not be
confused.
}

\section{Sub-national use}{

The primary intended use of poststratification in rbesd is sub-national
(admin1-level) analysis within a single country. In this setting, the
"country" grouping column in the model corresponds to the first
administrative unit (e.g. region or state), not a nation. All package
functionality works identically — only the interpretation differs.
Country-level poststratification across multiple countries is structurally
supported but is not the primary design intent.
}

\seealso{
\code{\link[=besd_fitted_probs]{besd_fitted_probs()}}, \code{\link[=besd_poststratify]{besd_poststratify()}}
}
