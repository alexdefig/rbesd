% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-missing.R
\name{besd_missing_summary}
\alias{besd_missing_summary}
\title{Summarise missing data fractions for regression variables}
\usage{
besd_missing_summary(x, vars = NULL, country_col = NULL, threshold = 0.05)
}
\arguments{
\item{x}{A \code{besd_data} object or plain data frame.}

\item{vars}{Character vector of variable names to check. If \code{NULL}, all cols checked.}

\item{country_col}{Name of the country column. Inferred automatically from
\code{besd_data} objects; required for plain data frames.}

\item{threshold}{Fraction above which a variable or the joint row is flagged.
Default \code{0.05}.}
}
\value{
A tibble with columns \code{variable}, \code{n_total}, \code{n_missing}, \code{pct_missing},
\code{flagged}, and a list-column \code{by_country} (per-country breakdown for each variable;
\code{NULL} for the joint row). The final row is always \code{"(listwise joint)"} and reflects
the fraction lost to complete-case deletion across all variables jointly.
}
\description{
Reports per-variable and joint (listwise) missing fractions. The joint fraction is
the proportion of rows that \code{complete.cases()} would drop across all variables
together — the number that actually matters for complete-case regression.
}
\details{
Call this after preparing your \code{besd_data} object but before \code{besd_regress()},
optionally re-running after applying \code{min_n_context} manually to see its impact on
the joint fraction.
}
