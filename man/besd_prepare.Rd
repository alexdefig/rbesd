% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regress-prep.R
\name{besd_prepare}
\alias{besd_prepare}
\title{Prepare a BeSD dataset for regression}
\usage{
besd_prepare(
  x,
  predictors,
  scope = c("by_country", "multilevel"),
  engine = c("frequentist", "bayes"),
  ref = c("mode", "first"),
  random_slopes = FALSE,
  correlated_re = FALSE,
  ref_levels = list(),
  min_n_context = 10L
)
}
\arguments{
\item{x}{A \code{besd_data} object.}

\item{predictors}{Character vector (\code{by_country}) or
\code{list(common = ..., context = ...)} (\code{multilevel}).}

\item{scope}{\code{"by_country"} or \code{"multilevel"}.}

\item{engine}{\code{"frequentist"} or \code{"bayes"}.}

\item{ref}{Reference rule: \code{"mode"} or \code{"first"}.}

\item{random_slopes}{If \code{TRUE} (multilevel), random slopes for common
predictors.}

\item{correlated_re}{If \code{TRUE} (multilevel Bayes), estimate correlations
between random effects.}

\item{ref_levels}{Optional named list of explicit reference labels
(multilevel common predictors only).}

\item{min_n_context}{Rare-level threshold for context predictors (default
\code{10L}). Observations in levels with fewer than this many observations
within a country are recoded to \code{NA} before fitting.}
}
\value{
A \code{besd_prep} object (a named list with class \code{"besd_prep"})
containing the encoded data frame, predictor metadata, level maps,
reference codes, term map, and all configuration fields needed for
fitting and tidying.
}
\description{
Encodes predictors, selects reference levels, applies \code{min_n_context}
rare-level collapsing, expands context dummies, and runs pre-flight checks
(invariant predictors, missingness warnings). Returns a \code{besd_prep} object
consumed by \code{besd_regress()} internally, or inspectable by the user before
fitting.
}
