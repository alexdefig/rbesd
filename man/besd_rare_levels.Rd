% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing.R
\name{besd_rare_levels}
\alias{besd_rare_levels}
\title{Inspect rare factor levels before fitting}
\usage{
besd_rare_levels(
  x,
  predictors,
  country_col = NULL,
  min_n = 10L,
  only_rare = TRUE
)
}
\arguments{
\item{x}{A \code{besd_data} object or plain data frame.}

\item{predictors}{Character vector of predictor column names to inspect.}

\item{country_col}{Name of the country column. Inferred automatically from
\code{besd_data} objects; required for plain data frames.}

\item{min_n}{Integer threshold to flag against (default \code{10L}). Sets the
\code{would_collapse} column — does not modify the data.}

\item{only_rare}{If \code{TRUE} (default), return only rows where \code{n < min_n}.
Set to \code{FALSE} to see all (predictor, country, level) combinations.}
}
\value{
A tibble with columns:
\describe{
\item{predictor}{Column name.}
\item{country}{Country label.}
\item{level}{Human-readable level label (or raw value if no label found).}
\item{n}{Observation count in this cell.}
\item{pct}{Fraction of that country's rows this cell represents.}
\item{would_collapse}{\code{TRUE} if \code{n < min_n} — this level would be
recoded to \code{NA} at the given threshold.}
\item{n_rows_affected}{Number of rows that would be recoded to \code{NA}
(equals \code{n} for levels that would collapse, \code{0L} otherwise).}
}
Ordered by \code{predictor}, \code{would_collapse} (TRUE first), then \code{n} ascending,
so the most dangerous cells are at the top within each predictor.
Returns a zero-row tibble if no factor predictors are found.
}
\description{
Reports per-(predictor, country, level) observation counts so you can make
an informed choice about \code{min_n_context} before calling \code{\link[=besd_prepare]{besd_prepare()}} or
\code{\link[=besd_regress]{besd_regress()}}. Levels with very few observations can cause near-complete
separation or wildly unstable estimates; levels below your chosen threshold
will be recoded to \code{NA} by \code{min_n_context} and excluded via listwise
deletion.
}
\details{
Only unordered factor and character columns are examined — continuous and
ordered predictors are skipped with a message.
}
\examples{
\dontrun{
# Check before choosing min_n_context
besd_rare_levels(my_data, predictors = c("dem_eth", "dem_rel"))

# See full breakdown including common levels
besd_rare_levels(my_data, predictors = "dem_eth", only_rare = FALSE)
}
}
