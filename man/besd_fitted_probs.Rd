% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/regress.R
\name{besd_fitted_probs}
\alias{besd_fitted_probs}
\title{Extract fitted probabilities from a BeSD regression model}
\usage{
besd_fitted_probs(fit, newdata = NULL, n_sample = 50L)
}
\arguments{
\item{fit}{A \code{besd_fit} or \code{besd_fit_by_country} object from
\code{\link[=besd_regress]{besd_regress()}}.}

\item{newdata}{\code{NULL} to predict on the training data, or a
\code{besd_poststrat_frame} object from \code{\link[=besd_poststrat_frame]{besd_poststrat_frame()}} to predict
on new data. Any other input will error with a clear message.}

\item{n_sample}{Integer. Number of posterior draws for Bayesian models.
For large datasets, keep this small (e.g. 50) to limit memory use.
Ignored for frequentist models.}
}
\value{
A \code{besd_fitted} object containing:
\describe{
\item{\code{draws}}{Named list, one element per outcome. Binary: matrix
\verb{[n_sample x n_obs]} (Bayes) or \verb{[1 x n_obs]} (frequentist). Ordinal:
3-D array \verb{[n_sample x n_obs x n_categories]}.}
\item{\code{meta}}{Model metadata: \code{engine}, \code{scope}, \code{outcomes}, \code{y_type},
\code{n_sample}, and \code{categories} (ordinal labels or \code{NULL} for binary).}
\item{\code{row_ids}}{Tibble with the country column and \code{.row_id} aligned to
the rows of \code{newdata} (or the training data if \code{newdata = NULL}).}
}
}
\description{
Returns fitted (predicted) probabilities from a \code{\link[=besd_regress]{besd_regress()}} output.
For Bayesian models, \code{n_sample} draws from the posterior distribution of
predicted probabilities are returned, enabling full uncertainty propagation
through downstream steps such as \code{\link[=besd_poststratify]{besd_poststratify()}}. For frequentist
models, a single point-estimate prediction is returned.
}
\details{
Poststratification (MrP) requires a Bayesian fit: passing a
\code{\link[=besd_poststrat_frame]{besd_poststrat_frame()}} as \code{newdata} for a frequentist model is an error,
because the resulting estimates would carry no uncertainty. Frequentist
fitted probabilities on the training data (\code{newdata = NULL}) remain
available for diagnostics, for binary outcomes.
}
\seealso{
\code{\link[=besd_poststrat_frame]{besd_poststrat_frame()}}, \code{\link[=besd_poststratify]{besd_poststratify()}}
}
