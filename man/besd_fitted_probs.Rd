% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poststratify-fitted.R
\name{besd_fitted_probs}
\alias{besd_fitted_probs}
\title{Evaluate a fitted BeSD model at poststratification cells}
\usage{
besd_fitted_probs(fit, newdata, n_sample = 50L)
}
\arguments{
\item{fit}{A \code{besd_fit} or \code{besd_fit_by_country} object from
\code{\link[=besd_regress]{besd_regress()}}, fitted with \code{engine = "bayes"}.}

\item{newdata}{A \code{besd_poststrat_frame} object from
\code{\link[=besd_poststrat_frame]{besd_poststrat_frame()}}. Required. Any other input will error with a
clear message.}

\item{n_sample}{Integer. Number of posterior draws to extract. For large
frames, keep this small (e.g. 50) to limit memory use.}
}
\value{
A \code{besd_fitted} object containing:
\describe{
\item{\code{draws}}{Named list, one element per outcome. Binary: matrix
\verb{[n_sample x n_cells]}. Ordinal: 3-D array
\verb{[n_sample x n_cells x n_categories]}.}
\item{\code{meta}}{Model metadata: \code{engine}, \code{scope}, \code{outcomes}, \code{y_type},
\code{y_types}, \code{n_sample}, and \code{categories} (ordinal labels or \code{NULL} for
binary).}
\item{\code{row_ids}}{Tibble with the country column and \code{.row_id} aligned to
the rows of \code{newdata}.}
}
}
\description{
Returns posterior draws of the predicted probabilities for every cell of a
\code{\link[=besd_poststrat_frame]{besd_poststrat_frame()}}. This is the second step of the MrP pipeline,
sitting between \code{\link[=besd_poststrat_frame]{besd_poststrat_frame()}} and \code{\link[=besd_poststratify]{besd_poststratify()}}: the
cells generally include covariate combinations that are rare or absent in
the survey, and this is where the model transfers information to them.
}
\details{
\code{n_sample} draws from the posterior distribution are returned, so that
\code{\link[=besd_poststratify]{besd_poststratify()}} can propagate the full distribution through the
weighting step and report credible intervals.

Poststratification requires a Bayesian fit. A frequentist model yields a
single point estimate per cell, so the poststratified estimates would carry
no uncertainty; this function errors rather than produce them. Refit with
\code{besd_regress(..., engine = "bayes")}.
}
\seealso{
\code{\link[=besd_poststrat_frame]{besd_poststrat_frame()}} for the preceding step,
\code{\link[=besd_poststratify]{besd_poststratify()}} for the following one, and \code{\link[=besd_regress]{besd_regress()}} for
fitting the model.
}
