% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/besd-summary.R
\name{summary.besd_data}
\alias{summary.besd_data}
\title{Summarise BeSD items by country}
\usage{
\method{summary}{besd_data}(
  object,
  method = c("survey"),
  items = NULL,
  conf_level = 0.95,
  include_demographics = TRUE,
  exclude_missing_tokens = FALSE,
  combine_top = FALSE,
  ci = TRUE,
  ...
)
}
\arguments{
\item{object}{A \code{besd_data} object.}

\item{method}{Summary method. Default is \code{survey}. Included for forward
compatibility.}

\item{items}{Optional character vector of item IDs to summarise.}

\item{conf_level}{Confidence level for intervals (default 0.95).}

\item{include_demographics}{If \code{TRUE} and demographics exist, also compute
raw (unweighted) percentages for demographic items and attach them as an
attribute (see \code{besd_demographics()}).}

\item{exclude_missing_tokens}{If \code{TRUE}, any \code{missing_tokens} supplied to
\code{as_besd()} are excluded from denominators even if they were kept as levels.}

\item{combine_top}{If \code{TRUE}, collapse the top-box levels (\code{toplevs} from the
item dictionary) into a single combined response per item.}

\item{ci}{If \code{TRUE} (default), compute survey-design confidence intervals
(\code{lcl}, \code{ucl}) via \code{\link[survey:svyciprop]{survey::svyciprop()}}. If \code{FALSE}, report weighted
proportions only with \code{lcl}/\code{ucl} set to \code{NA} — this skips the survey
design entirely, so it is robust to thin cells and emits no \code{glm}
convergence warnings (used by \code{\link[=besd_summary_by]{besd_summary_by()}}).}

\item{...}{Unused; included for S3 compatibility.}
}
\value{
A tibble with columns including \code{country}, \code{item_id}, \code{response},
\code{pct}, \code{lcl}, \code{ucl}, and metadata columns from the item dictionary. If
\code{include_demographics = TRUE}, a demographics tibble is attached as an
attribute (see \code{besd_demographics()}).
}
\description{
Computes weighted response percentages by country (or national total if no
country column is available). Optionally adds confidence intervals for each
response category.
}
\details{
Confidence intervals are computed on proportions and returned as percentages
(\code{lcl}, \code{ucl} are on the 0--100 scale).
}
