% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/missing.R
\name{besd_recode_levels}
\alias{besd_recode_levels}
\title{Recode factor levels in a column before or after calling as_besd()}
\usage{
besd_recode_levels(x, recode, col = NULL, warn_new = TRUE)
}
\arguments{
\item{x}{A \code{besd_data} object or a factor/character vector.}

\item{recode}{Named character vector: names are old labels, values are new
labels. E.g. \code{c("Luo" = "Other", "Kikuyu minority" = "Other")}.}

\item{col}{Column name to recode. Required when \code{x} is a \code{besd_data} object;
ignored when \code{x} is a vector.}

\item{warn_new}{If \code{TRUE} (default), warn when a target label does not
already exist in the data, as this may indicate a dictionary mismatch.}
}
\value{
\code{x} with levels recoded. When \code{x} is a \code{besd_data} object, the
dictionary is updated and a \code{besd_data} object is returned. When \code{x} is a
vector, a recoded vector of the same type is returned.
}
\description{
A lightweight helper for collapsing or renaming rare levels. Accepts either
a \code{besd_data} object (typical use: after \code{\link[=as_besd]{as_besd()}}, before \code{\link[=besd_regress]{besd_regress()}})
or a plain vector (use: before \code{\link[=as_besd]{as_besd()}}).
}
\details{
When \code{x} is a \code{besd_data} object, the dictionary levels for \code{col} are also
updated to reflect the recoding, so the object stays internally consistent.

The target label must already exist in the data or dictionary. Recoding to a
brand-new label not in the dictionary will cause an unknown-value error if
the object is later passed back through \code{\link[=as_besd]{as_besd()}}.
}
\examples{
\dontrun{
# On a besd_data object (recommended)
y <- besd_recode_levels(
  y,
  col    = "dem_eth",
  recode = c("Luo" = "Other", "Kikuyu minority" = "Other")
)

# On a raw vector before as_besd()
data_demo$dem_eth <- besd_recode_levels(
  data_demo$dem_eth,
  recode = c("Luo" = "Other", "Kikuyu minority" = "Other")
)
}
}
