% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/poststratify.R
\name{besd_poststratify}
\alias{besd_poststratify}
\title{Poststratify BeSD fitted probabilities to a target population}
\usage{
besd_poststratify(fitted, poststrat_frame, by = NULL, conf_level = 0.95)
}
\arguments{
\item{fitted}{A \code{besd_fitted} object from \code{\link[=besd_fitted_probs]{besd_fitted_probs()}}.}

\item{poststrat_frame}{A \code{besd_poststrat_frame} object from
\code{\link[=besd_poststrat_frame]{besd_poststrat_frame()}}. Row order must correspond to the \code{newdata}
used when calling \code{\link[=besd_fitted_probs]{besd_fitted_probs()}}.}

\item{by}{Optional character vector of column names in \code{poststrat_frame}
to stratify estimates by (e.g. \code{"dem_gen"} to return separate
poststratified estimates per gender within each country). Columns must
be present in \code{poststrat_frame}.}

\item{conf_level}{Numeric. Credible interval width for Bayesian models.
Default \code{0.95}. Ignored for frequentist models.}
}
\value{
A \code{besd_poststrat} tibble with columns: grouping columns (country
and any \code{by} variables with human-readable labels), \code{outcome}, \code{estimate},
\code{lower}, \code{upper}. For ordinal outcomes a \code{category} column is also
included. \code{lower} and \code{upper} are \code{NA} for frequentist models.
}
\description{
Applies population counts from a \code{\link[=besd_poststrat_frame]{besd_poststrat_frame()}} to fitted
probabilities from \code{\link[=besd_fitted_probs]{besd_fitted_probs()}} to produce poststratified
(population-representative) estimates. For Bayesian models, the full
posterior distribution is propagated through the weighting step, yielding
median estimates and credible intervals. For frequentist models, a single
weighted point estimate is returned with no uncertainty quantification.
}
\section{Sub-national use}{

The primary intended use of poststratification in rbesd is sub-national
(admin1-level) analysis within a single country. In this setting, the
"country" grouping column corresponds to the first administrative unit
(e.g. region or state). Country-level poststratification across multiple
countries is structurally supported but is not the primary design intent.
}

\seealso{
\code{\link[=besd_poststrat_frame]{besd_poststrat_frame()}}, \code{\link[=besd_fitted_probs]{besd_fitted_probs()}}
}
