% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/besd-summary.R
\name{besd_summary_by}
\alias{besd_summary_by}
\title{Summarise BeSD items stratified by a demographic variable}
\usage{
besd_summary_by(object, by_col, ...)
}
\arguments{
\item{object}{A \code{besd_data} object.}

\item{by_col}{Character scalar.  Name of the demographic column in
\code{object} to stratify by (e.g. \code{"dem_gen"}).}

\item{...}{Additional arguments forwarded to \code{summary.besd_data} (e.g.
\code{conf_level}, \code{exclude_missing_tokens}).}
}
\value{
A tibble with the same columns as \code{summary(besd_data)} plus:
\describe{
\item{subgroup_var}{The column name passed to \code{by_col}.}
\item{subgroup_label}{Human-readable label from \code{dem_dict} if available,
otherwise equal to \code{subgroup_var}.}
\item{subgroup_level}{The specific level of \code{by_col} for each row.}
}
}
\description{
Produces a per-stratum summary table identical in structure to
\code{summary(besd_data)} but broken down by the levels of a single demographic
variable.  All existing logic (weights, CIs, missing-token handling, item
types) is preserved because the function works by temporarily using the
combined \verb{(country × dem_level)} key as the grouping variable and then
splitting it back.
}
