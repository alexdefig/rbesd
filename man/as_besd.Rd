% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/besd-as_besd.R
\name{as_besd}
\alias{as_besd}
\title{Prepare and standardise WHO/UNICEF BeSD data}
\usage{
as_besd(
  df,
  besd_dict = besd_dictionary(),
  dem_dict = NULL,
  mapping = NULL,
  multichoice_specs = list(),
  country_col = "country",
  stratum_col = NULL,
  psu_col = NULL,
  weight_col = NULL,
  id_col = NULL,
  keep_original = FALSE,
  missing_tokens = NULL,
  missing_action = c("na", "keep"),
  unknown_action = c("error", "na"),
  warn_on_unknown = TRUE
)
}
\arguments{
\item{df}{A data frame containing raw BeSD survey data.}

\item{besd_dict}{BeSD item dictionary.}

\item{dem_dict}{Optional: predictor dictionary.}

\item{mapping}{Named character vector: names=raw variables; values=standard item_id.}

\item{multichoice_specs}{Specs for multichoice when multiple raw cols map to one
item_id.}

\item{country_col}{Country column name.}

\item{stratum_col}{Optional respondent sampling stratum column. Stratum are used by
\code{summary.besd_data()} when computing design-adjusted summaries but are
\strong{not} currently used by \code{besd_regress()} (see its documentation).}

\item{psu_col}{Optional respondent sampling PSU column. PSUs are used by
\code{summary.besd_data()} when computing design-adjusted summaries, but are
\strong{not} currently used by \code{besd_regress()} (see its documentation).}

\item{weight_col}{Optional name of the survey weight column in \code{df}. Weights are
used by \code{summary.besd_data()} when computing design-adjusted summaries, but are
\strong{not} currently used by \code{besd_regress()} (see its documentation).}

\item{id_col}{Optional respondent id column.}

\item{keep_original}{If TRUE, keep all original columns (plus harmonised ones).}

\item{missing_tokens}{Specifies which response values should be treated as
missing data. Accepts two forms:
\itemize{
\item \strong{Character vector}: the same tokens are applied to every item, e.g.
\code{missing_tokens = c("Don't know", "Refused")}.
\item \strong{Named list}: per-item token vectors, where names are \code{item_id} values.
The special name \code{.all} defines a cross-item default that is \emph{combined}
(not replaced) with any item-specific entries. For example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{missing_tokens = list(
  .all      = "Don't know",        # applied to every item
  sp_peer   = c("Refused", "N/A"), # additionally for sp_peer only
  tf_safety = "Not applicable"     # additionally for tf_safety only
)
}\if{html}{\out{</div>}}

Here \code{sp_peer} treats \code{"Don't know"}, \code{"Refused"}, and \code{"N/A"} all as
missing; \code{tf_safety} treats \code{"Don't know"} and \code{"Not applicable"} as
missing; all other items treat only \code{"Don't know"} as missing.
}}

\item{missing_action}{One of \code{"na"} (default; recode missing tokens to \code{NA}
after coercion) or \code{"keep"} (retain them as explicit trailing factor levels).
Use \code{"keep"} when you want missing-response rows to appear in
\code{summary.besd_data()} output as distinct categories.}

\item{unknown_action}{One of \code{"error"} (default; stop if values not in
dictionary) or \code{"na"} (coerce unknown/unmapped values to \code{NA} with an
optional warning). Note: \code{missing_tokens} are allow-listed and never
treated as unknown, regardless of this setting.}

\item{warn_on_unknown}{If \code{TRUE} (default), emit a warning when
\code{unknown_action = "na"} coerces unrecognised values.}
}
\value{
A \code{besd_data} object.
}
\description{
Prepare and standardise WHO/UNICEF BeSD data
}
\section{Missing data handling}{

\code{as_besd()} distinguishes three kinds of non-response:
\enumerate{
\item \strong{System-missing} (\code{NA} in input): passed through as \code{NA} throughout;
no special treatment required.
\item \strong{Missing tokens} — survey-specific non-response codes (\code{"Don't know"},
\code{"Refused"}, etc.) declared via \code{missing_tokens}. With
\code{missing_action = "na"} these become \code{NA}; with \code{"keep"} they are
retained as explicit factor levels so they remain visible in summaries.
The choice depends on whether you want to count or display non-response.
\item \strong{Unknown values} — responses absent from the dictionary entirely
(data-entry errors, unmapped codes). Controlled by \code{unknown_action}.
}

In \code{summary.besd_data()}, the \code{n} column reflects valid responses \emph{after}
missing-token exclusion (when \code{exclude_missing_tokens = TRUE}, the default).
There is currently no dedicated \code{n_missing} column; if you need to report
non-response rates, use \code{missing_action = "keep"} so that missing-token rows
appear explicitly in the summary output.
}

\examples{
\dontrun{
# 1. Uniform missing tokens across all items
x <- as_besd(
  df,
  missing_tokens = c("Don't know", "Refused"),
  missing_action = "na"
)

# 2. Per-variable missing tokens with a cross-item default
x <- as_besd(
  df,
  missing_tokens = list(
    .all      = "Don't know",          # every item
    sp_peer   = c("Refused", "N/A"),   # sp_peer gets these too
    tf_safety = "Not applicable"       # tf_safety gets this too
  ),
  missing_action = "na"
)

# 3. Keep missing tokens as explicit levels (they appear in summary output)
x <- as_besd(
  df,
  missing_tokens = "Don't know",
  missing_action = "keep"
)
summary(x)  # "Don't know" appears as a response row with its own pct

# 4. Recode missing tokens later (e.g. after inspecting the data)
x <- as_besd(df, missing_action = "keep", missing_tokens = "Don't know")
x <- besd_recode_missing(x, tokens = "Don't know")
}
}
